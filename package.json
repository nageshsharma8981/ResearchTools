{
  "name": "rewiseed-research-tools",
  "version": "1.0.0",
  "private": true,
  "description": "ReWiseEd Research Tools — offline-first, bring-your-own-key research suite",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard --no-port-switching"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
